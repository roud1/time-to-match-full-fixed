"use client"

type DatingHeroFloatsProps = {
  scrollProgress: import("motion/react").MotionValue<number>
}

/** Sofia peek is rendered inside DatingHeroMatchPreview — floats disabled. */
export function DatingHeroFloats(_props: DatingHeroFloatsProps) {
  return null
}
