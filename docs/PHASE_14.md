# Phase 14 — Emotional Intelligence Platform Layer

Time to Match understands relationships — not just matches.

## Intelligence stack (`lib/intelligence/`)

| Module | Purpose |
|--------|---------|
| `compatibility-engine.ts` | Score, resonance, long-term potential, tempo, depth, balance |
| `relationship-rhythm.ts` | Slow burn, intense chemistry, calm, unstable, imbalance |
| `connection-states.ts` | Resonance, cognitive alignment, attachment, distance, stable rhythm |
| `connection-forecast.ts` | Atmospheric forecasts (not toxic predictions) |
| `personality-mirror.ts` | “How AI sees this bond” mirror lines |
| `memory-intelligence.ts` | Peaks, nights, attachment, turning points |
| `ui-intelligence.ts` | Adaptive glow, motion, mood for UI |
| `analyze.ts` | `analyzeConnectionIntelligence()` — single entry |

## UI (`components/intelligence/`)

- `IntelligenceInsightStack` — chat header intelligence panel
- `IntelligentSpaceLayer` — evolving shared space aura
- `useConnectionIntelligence` — hook

## Positioning

**Emotional intelligence platform** · relationship operating system · AI-driven intimacy ecosystem.

Not: dating app, messenger, social network.

## Production

Builds on Phase 12 world + Phase 13 network. Server AI via `/api/analyze-connection`; client intelligence is deterministic + extensible for model scores.
